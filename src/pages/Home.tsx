import { Header, Navbar } from "@/components";

const Home = () => {
  return (
    <div>
      <Header transparent={true} height={100}>
        <Navbar />
      </Header>
    </div>
  );
};

export default Home;
